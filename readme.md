# LITLE PENGU TOKEN and PRESALE CONTRACT

```js
  // To be used on testnet
  function getLatestPrice() public pure returns (uint256) {
        // testnet 
        uint256 price = 4466 * 1e18;
        return price;
    }
```